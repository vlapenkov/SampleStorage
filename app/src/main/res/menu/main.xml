<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".activities.ProductsActivity">
    <item android:id="@+id/search"
        android:title="@string/hint_search"
        android:icon="@android:drawable/ic_menu_search"
        app:showAsAction="collapseActionView|ifRoom"
        app:actionViewClass="android.support.v7.widget.SearchView" />
<item
    android:id="@+id/toPreferences"
    android:orderInCategory="100"
    android:title="To preferences"
    app:showAsAction="never" />
<item
android:id="@+id/refresh"
android:orderInCategory="50"
android:title="Refresh list"
app:showAsAction="never" />
<item
android:id="@+id/load_entities"
android:orderInCategory="80"
android:title="Load entities"
app:showAsAction="never" />
    <item
        android:id="@+id/toStockCells"
        android:orderInCategory="60"
        android:title="To stock cells"
        app:showAsAction="never" />
</menu>